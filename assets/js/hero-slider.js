(()=>{const slider=document.querySelector('.hero-slider');if(!slider)return;const slides=[...slider.querySelectorAll('.hero-slide')],dots=[...slider.querySelectorAll('.hero-dot')],prev=slider.querySelector('.hero-prev'),next=slider.querySelector('.hero-next'),reduceMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;let current=0,timer=null,touchX=null;const delay=7000;const stop=()=>{if(timer)clearInterval(timer);timer=null};const start=()=>{stop();if(!reduceMotion&&slides.length>1)timer=setInterval(()=>render(current+1,false),delay)};const render=(index,restart=true)=>{current=(index+slides.length)%slides.length;slides.forEach((slide,i)=>{const active=i===current;slide.classList.toggle('is-active',active);slide.setAttribute('aria-hidden',active?'false':'true')});dots.forEach((dot,i)=>{const active=i===current;dot.classList.toggle('is-active',active);dot.setAttribute('aria-selected',active?'true':'false')});if(restart)start()};prev?.addEventListener('click',()=>render(current-1));next?.addEventListener('click',()=>render(current+1));dots.forEach((dot,i)=>dot.addEventListener('click',()=>render(i)));slider.addEventListener('mouseenter',stop);slider.addEventListener('mouseleave',start);slider.addEventListener('focusin',stop);slider.addEventListener('focusout',start);slider.addEventListener('touchstart',e=>{touchX=e.changedTouches[0]?.clientX??null},{passive:true});slider.addEventListener('touchend',e=>{if(touchX===null)return;const endX=e.changedTouches[0]?.clientX??touchX,delta=endX-touchX;touchX=null;if(Math.abs(delta)>48)render(current+(delta<0?1:-1))},{passive:true});render(0)})();
